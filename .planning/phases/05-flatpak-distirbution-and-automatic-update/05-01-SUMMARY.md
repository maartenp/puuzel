---
phase: 05-flatpak-distirbution-and-automatic-update
plan: 01
subsystem: infra
tags: [flatpak, appstream, desktop-entry, release-script, cargo, rust, linux]

# Dependency graph
requires:
  - phase: 02-playable-game
    provides: working binary + data/puuzel.db + data/DejaVuSans.ttf to bundle
provides:
  - Flatpak manifest (org.freedesktop.Sdk 24.08 + rust-stable, buildsystem: simple, offline cargo build)
  - AppStream metainfo.xml (minimal valid, component type=desktop-application)
  - Desktop entry file (Exec=puuzel, Categories=Game;BoardGame;)
  - release.sh interactive release script (version bump, commit, tag, push)
  - DIST-04 verified: zero git deps in Cargo.lock
affects: [05-02-update-check, 05-03-ci-workflow, phase-04-macos]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Flatpak buildsystem:simple with cargo --offline fetch + build
    - Portable sed version bump (macOS -i '' / Linux -i)
    - Interactive release script reading version from Cargo.toml

key-files:
  created:
    - flatpak/io.github.maartenp.puuzel.yml
    - flatpak/io.github.maartenp.puuzel.metainfo.xml
    - flatpak/io.github.maartenp.puuzel.desktop
    - release.sh
  modified: []

key-decisions:
  - "app-id is io.github.maartenp.puuzel (reverse-DNS with GitHub hosting per D-CONTEXT)"
  - "runtime-version 24.08 selected (23.08 EOL); extension branch must match exactly"
  - "--share=network in finish-args for startup version check (D-06)"
  - "cargo-sources.json listed in manifest sources but NOT committed — CI generates it fresh (D-13)"
  - "release.sh uses portable sed with OS detection for cross-platform compatibility"
  - "CARGO_HOME set to /run/build/puuzel/cargo to avoid config conflict pitfall"

patterns-established:
  - "Flatpak manifest Pattern: buildsystem:simple, sources=[dir:.., cargo-sources.json], cargo --offline build"
  - "Release Pattern: read version from Cargo.toml, IFS split, increment, portable sed rewrite, cargo check, git add/commit/tag/push"

requirements-completed: [DIST-01, DIST-04]

# Metrics
duration: 8min
completed: 2026-03-22
---

# Phase 05 Plan 01: Flatpak Packaging Files Summary

**Flatpak manifest, AppStream metainfo.xml, desktop entry, and interactive release.sh for offline Rust build with org.freedesktop.Sdk 24.08**

## Performance

- **Duration:** 8 min
- **Started:** 2026-03-22T17:21:31Z
- **Completed:** 2026-03-22T17:30:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Created `flatpak/io.github.maartenp.puuzel.yml` with buildsystem:simple, CARGO_NET_OFFLINE, --share=network finish-arg, and install commands for binary, data, and font
- Created `flatpak/io.github.maartenp.puuzel.metainfo.xml` (minimal valid AppStream component) and `flatpak/io.github.maartenp.puuzel.desktop` (Exec=puuzel, Categories=Game;BoardGame;)
- Created `release.sh` with interactive M/m/p version bump, portable sed, cargo check, git commit/tag/push
- Verified DIST-04: Cargo.lock contains zero git-sourced dependencies

## Task Commits

Each task was committed atomically:

1. **Task 1: Flatpak manifest, AppStream metainfo, and desktop entry** - `cc5be7f` (feat)
2. **Task 2: release.sh and DIST-04 verification** - `fea3d09` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `flatpak/io.github.maartenp.puuzel.yml` - Flatpak build manifest (offline cargo build, SDK 24.08, data file installs)
- `flatpak/io.github.maartenp.puuzel.metainfo.xml` - AppStream metadata for GNOME Software / Flathub compatibility
- `flatpak/io.github.maartenp.puuzel.desktop` - Desktop entry for launcher integration
- `release.sh` - Interactive release script (read version, bump, portable sed, cargo check, commit, tag, push)

## Decisions Made

- Application ID set to `io.github.maartenp.puuzel` (reverse-DNS convention with GitHub hosting)
- Runtime version `24.08` selected — 23.08 is EOL in 2026
- `CARGO_HOME` explicitly set to `/run/build/puuzel/cargo` to avoid the documented cargo config conflict pitfall when flatpak-cargo-generator writes `.cargo/config.toml`
- `cargo-sources.json` listed as manifest source but deliberately not committed (CI generates it from Cargo.lock per D-13)
- `release.sh` uses OS detection for portable `sed -i` (macOS needs `''`, Linux does not)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Flatpak packaging files ready; Plan 02 (in-app update check) and Plan 03 (CI workflow) can proceed
- `cargo-sources.json` must be generated by CI from `Cargo.lock` before a real Flatpak build — this is the intended workflow (D-13)
- Data file path fix (`/app/share/puuzel/puuzel.db` vs `data/puuzel.db`) is a known issue documented in the research (Pitfall 7); Plan 02 or a follow-up task should address it in `src/main.rs`

---
*Phase: 05-flatpak-distirbution-and-automatic-update*
*Completed: 2026-03-22*

## Self-Check: PASSED

- FOUND: flatpak/io.github.maartenp.puuzel.yml
- FOUND: flatpak/io.github.maartenp.puuzel.metainfo.xml
- FOUND: flatpak/io.github.maartenp.puuzel.desktop
- FOUND: release.sh
- FOUND: .planning/phases/05-flatpak-distirbution-and-automatic-update/05-01-SUMMARY.md
- FOUND commit cc5be7f (Task 1)
- FOUND commit fea3d09 (Task 2)
